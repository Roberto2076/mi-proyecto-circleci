version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run: echo "🚀 Hola Mundo desde CircleCI, todo funciona!"

workflows:
  version: 2
  build_and_test:
    jobs:
      - build
